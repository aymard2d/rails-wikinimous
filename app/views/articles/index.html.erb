<h1>My Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article)%>
    </li>
  <% end %>
</ul>

<li> <%= link_to "Ajouter un nouvel article", new_article_path(@article) %> </li>
